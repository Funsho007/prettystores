<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p>Price: <%= number_to_currency(@product.price) %></p>
<%= form_with url: add_item_cart_path(product_id: @product.id), method: :post do %>
  <%= submit_tag 'Add to Cart' %>
<% end %>
<%= link_to 'Back to Products', products_path %>
